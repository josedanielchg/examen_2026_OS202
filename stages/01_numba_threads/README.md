# Stage 01 Numba Threads

Reservado para la etapa donde se añadirá paralelización con `numba`, `parallel=True` y `prange`.

## Regla

Cuando esta etapa empiece, debe partir de una copia nueva de la etapa anterior, sin editar `stages/00_baseline/`.
